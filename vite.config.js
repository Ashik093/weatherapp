import { defineConfig } from 'vite';

export default defineConfig({
    // Configuration options go here
    // For example:
    build: {
        outDir: 'dist',
        minify: true,
        target: 'esnext'
    },
});